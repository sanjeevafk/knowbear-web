services:
  knowbear:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: knowbear-local
    ports:
      - "5173:5173"
      - "8000:8000"
    environment:
      - VITE_API_URL=http://localhost:8000
    volumes:
      - .:/app
      - /app/node_modules
